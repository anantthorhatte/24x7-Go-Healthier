function showAlert() {
    // Get the value of the input field
    var searchTerm = document.getElementById("loginnInput").value;
    
    // Display an alert with the search term
    alert("Welcome " + searchTerm );
}