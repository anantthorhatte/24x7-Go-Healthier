
function scheduleAppointment(){
  const takeAppointment = document.getElementById("appointmentDate").value;
  console.log(takeAppointment);

  const currentDate = new Date();
 
 /* if()
  {
   document.write(` Appointment date scheduled successfully `);
  }
  else
  {
    document.write(` Appointment date hasn't scheduled successfully `);
  } 
  */
}